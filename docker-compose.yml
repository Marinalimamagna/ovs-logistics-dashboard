version: '3.8'

services:
  frontend:
    build: .
    container_name: ovs-logistics-frontend
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/.next
      - /app/node_modules
    environment:
      - NODE_ENV=development